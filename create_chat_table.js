const { Client } = require('pg'); const c = new Client({ connectionString: process.env.DATABASE_URL }); c.connect().then(async () => { try { await c.query(CREATE TABLE IF NOT EXISTS bota_chat_messages (id SERIAL PRIMARY KEY, user_name VARCHAR(100), wallet_address VARCHAR(100), message TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP)); console.log('Table bota_chat_messages created or exists.'); } catch(e) { console.error(e); } c.end(); });
